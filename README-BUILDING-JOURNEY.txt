MUSA3D — Cinematic Building Journey (Way 1)

This package is built from your uploaded current musa3d folder.
It keeps your SEO, teachers, teacher order, Nadia photo, social links, app section and Vercel config.

New flow:
Galaxy / outer scene -> approach futuristic glass MUSA3D building -> entrance doors open -> branded lobby -> Hero -> Services -> Teachers -> App -> Results -> Footer.

The lobby contains MUSA3D branding on wall screens, the main screen, floor glass mark and entrance gate.
The rest of the website gets a lightweight fake-3D architectural room shell so the same idea continues without heavy 3D models.

REPLACE ONLY:
- index.html
- styles.css
- app.js

Then run:
git add -A
git commit -m "Add MUSA3D building journey"
git push
